name: Daily upstream build

on:
  workflow_dispatch:
  schedule:
    - cron: "0 21 * * *"

permissions:
  contents: write

jobs:
  sync-upstream:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.sync.outputs.changed }}
      tag_name: ${{ steps.tag.outputs.tag_name }}
      upstream_short: ${{ steps.sync.outputs.upstream_short }}

    steps:
      - name: Checkout build branch
        uses: actions/checkout@v4
        with:
          ref: build/win-mac
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Fetch branches
        run: |
          git remote add upstream https://github.com/BigPizzaV3/CodexPlusPlus.git
          git fetch upstream main
          git fetch origin build/win-mac

      - name: Sync build branch
        id: sync
        run: |
          set -euo pipefail
          upstream="$(git rev-parse upstream/main)"
          echo "upstream_short=$(git rev-parse --short upstream/main)" >> "$GITHUB_OUTPUT"

          git checkout -B build/win-mac origin/build/win-mac
          if git merge-base --is-ancestor "$upstream" HEAD; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "build/win-mac already contains upstream main."
            exit 0
          fi

          git merge --no-edit upstream/main
          git push origin build/win-mac

          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Create daily tag
        id: tag
        if: steps.sync.outputs.changed == 'true'
        run: |
          set -euo pipefail
          tag_name="auto-build-$(date -u +'%Y%m%d-%H%M%S')"
          git tag "$tag_name"
          git push origin "$tag_name"
          echo "tag_name=$tag_name" >> "$GITHUB_OUTPUT"

  build-windows:
    needs: sync-upstream
    if: needs.sync-upstream.outputs.changed == 'true'
    runs-on: windows-latest

    steps:
      - name: Checkout build branch
        uses: actions/checkout@v4
        with:
          ref: build/win-mac

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build Windows app
        shell: pwsh
        run: .\scripts\build_windows.ps1

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: CodexPlusPlus-windows
          path: dist/CodexPlusPlus-windows.zip

  build-macos:
    needs: sync-upstream
    if: needs.sync-upstream.outputs.changed == 'true'
    runs-on: macos-latest

    steps:
      - name: Checkout build branch
        uses: actions/checkout@v4
        with:
          ref: build/win-mac

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build macOS app
        run: bash scripts/build_macos.sh

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: CodexPlusPlus-macos
          path: |
            dist/CodexPlusPlus-macos.zip
            dist/CodexPlusPlus-macos.dmg

  publish-release:
    needs:
      - sync-upstream
      - build-windows
      - build-macos
    if: needs.sync-upstream.outputs.changed == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.sync-upstream.outputs.tag_name }}
          name: ${{ needs.sync-upstream.outputs.tag_name }}
          body: |
            Automated build from upstream main ${{ needs.sync-upstream.outputs.upstream_short }}.
          files: |
            release-assets/CodexPlusPlus-windows/CodexPlusPlus-windows.zip
            release-assets/CodexPlusPlus-macos/CodexPlusPlus-macos.zip
            release-assets/CodexPlusPlus-macos/CodexPlusPlus-macos.dmg

  notify-success:
    needs:
      - sync-upstream
      - publish-release
    if: needs.sync-upstream.outputs.changed == 'true' && needs.publish-release.result == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Send success email
        uses: dawidd6/action-send-mail@v6
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: CodexPlusPlus daily build succeeded
          to: ozonect@qq.com
          from: ${{ secrets.SMTP_USERNAME }}
          body: |
            Upstream changed and the Windows/macOS build succeeded.

            Release:
            https://github.com/wuliangtdi/CodexPlusPlus/releases/tag/${{ needs.sync-upstream.outputs.tag_name }}

            Upstream main:
            ${{ needs.sync-upstream.outputs.upstream_short }}

  notify-failure:
    needs:
      - sync-upstream
      - build-windows
      - build-macos
      - publish-release
    if: always() && (needs.sync-upstream.result == 'failure' || needs.build-windows.result == 'failure' || needs.build-macos.result == 'failure' || needs.publish-release.result == 'failure')
    runs-on: ubuntu-latest

    steps:
      - name: Send failure email
        uses: dawidd6/action-send-mail@v6
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: CodexPlusPlus daily build failed
          to: ozonect@qq.com
          from: ${{ secrets.SMTP_USERNAME }}
          body: |
            The daily upstream sync/build failed.

            Workflow run:
            https://github.com/wuliangtdi/CodexPlusPlus/actions/runs/${{ github.run_id }}

            Job results:
            sync-upstream: ${{ needs.sync-upstream.result }}
            build-windows: ${{ needs.build-windows.result }}
            build-macos: ${{ needs.build-macos.result }}
            publish-release: ${{ needs.publish-release.result }}

            Common cause:
            If sync-upstream failed after an upstream change, build/win-mac may have merge conflicts and needs manual resolution.
